Static deployment of the backtest report. `index.html` is generated by
`scripts/deploy_report.sh` (gitignored). No password: the URL is unlisted but
public to anyone who has it; search engines are told not to index it.
